[
  {
    "feature_id": "infra-004",
    "status": "pass",
    "category": "data",
    "tested_steps": [
      "Reviewed prd.json item infra-004 and confirmed expected scope: S3 uploads, download URLs, and MIME restrictions for image/*, .html, and .pdf.",
      "Ran targeted regression: npx vitest run tests/s3.test.ts, 17/17 tests passed after adding coverage for unsupported content types and HTML charset handling.",
      "Ran full validation: make check passed and make test passed with 35/35 files and 390/390 tests green.",
      "Ran smoke E2E: npx playwright test tests/e2e/smoke.spec.ts, 5/5 tests passed.",
      "Ran real AWS S3 verification using the compiled src/lib/s3.ts module: uploaded PDF, HTML, HTML with charset, and PNG objects, fetched each through presigned URLs with HTTP 200, and deleted all test objects successfully.",
      "Verified unsupported MIME handling end-to-end: text/plain upload now rejects with 'contentType must be image/*, text/html, or application/pdf'."
    ],
    "bugs_found": [
      {
        "severity": "major",
        "description": "uploadFile accepted unsupported MIME types such as text/plain even though the PRD limits support to image/*, text/html, and application/pdf.",
        "expected": "Unsupported MIME types should be rejected before the S3 PutObject request is sent.",
        "actual": "A real text/plain upload succeeded against S3 before the fix.",
        "reproduction": "Call uploadFile({ key: 'qa/sample.txt', body: Buffer.from('plain text'), contentType: 'text/plain' }) and observe that it uploads instead of rejecting."
      }
    ],
    "fixes_applied": [
      "Added MIME validation in src/lib/s3.ts to allow only image/*, text/html, and application/pdf, including text/html with charset parameters.",
      "Added regression tests for unsupported MIME rejection and HTML content type with charset in tests/s3.test.ts."
    ]
  },
  {
    "feature_id": "layout-001",
    "status": "pass",
    "category": "layout",
    "tested_steps": [
      "Reviewed prd.json item layout-001 and confirmed the app shell requirements: 10 sidebar nav items, persistent sidebar, active nav highlighting, and route changes across dashboard pages.",
      "Ran automated regression before and after the fix: make test passed with 35/35 files and 392/392 tests green.",
      "Ran Playwright smoke against an isolated webpack dev server on http://localhost:3016 after the fix: tests/e2e/smoke.spec.ts passed 5/5.",
      "Ran an Ever CLI manual pass on the isolated server: ever start --url http://localhost:3016/, ever snapshot --mode full to verify all 10 nav items, ever eval to route to /domains and /api-keys, then ever snapshot and ever screenshot to confirm the sidebar persisted and the destination pages rendered.",
      "Captured Ever CLI screenshots during the manual pass to confirm the Emails, Domains, and API Keys dashboard states after navigation."
    ],
    "bugs_found": [
      {
        "severity": "major",
        "description": "Navigating from the sidebar to /api-keys crashed the page when the database connection failed instead of rendering the API Keys shell with an empty state.",
        "expected": "Sidebar navigation should route to /api-keys and keep the app shell usable even if API key data cannot be loaded.",
        "actual": "Ever CLI navigation surfaced a server error page with digest 289396507 caused by a Postgres password authentication failure in src/app/(dashboard)/api-keys/page.tsx.",
        "reproduction": "Start the local app, open /emails, navigate to /api-keys from the sidebar, and observe that the page throws before rendering the API Keys heading when DATABASE_URL rejects auth."
      }
    ],
    "fixes_applied": [
      "Wrapped the API Keys server-page data load in a try/catch and fell back to empty key/domain lists so the page still renders the layout shell and empty state when the database is unavailable.",
      "Strengthened tests/e2e/smoke.spec.ts so the sidebar persistence flow now verifies the /api-keys URL and the exact API Keys heading."
    ]
  }
]
