## Iteration 1 — infra-001: Database Schema (2026-03-29)

### What was built
- Complete Drizzle ORM schema with 14 tables: domains, api_keys, emails, email_events, segments, topics, contacts, contact_segments, contact_topics, broadcasts, webhooks, templates, properties, logs
- 7 PostgreSQL enums: email_status (12 values), domain_status (5), permission_type (2), broadcast_status (5), topic_visibility (2), topic_default_subscription (2), webhook_event (17)
- Full relational mappings between all tables (foreign keys + Drizzle relations)
- Database client with SSL support for RDS connections
- Generated migration SQL and applied to RDS Postgres (14 tables created)

### Test results
- 22 unit tests passing (schema validation: table names, columns, enums, constraints)
- make check: typecheck + lint passing
- make test: all unit tests passing

### Decisions
- Used `sslmode=no-verify` for RDS connection (Amazon CA cert not in local trust store)
- Applied migration SQL directly after schema reset (pre-existing tables from earlier attempts conflicted)
- Added SSL config to both drizzle.config.ts and db/index.ts runtime client

### Files changed
- src/lib/db/schema.ts — Complete schema with all tables, enums, relations
- src/lib/db/index.ts — DB client with SSL support
- drizzle.config.ts — Updated with SSL handling for RDS
- drizzle/0000_fearless_boomer.sql — Generated migration
- tests/schema.test.ts — 22 unit tests for schema validation

## Iteration 2 — infra-002: AWS SES Email Sending Client (2026-03-29)

### What was built
- SES client wrapper (`src/lib/ses.ts`) around `@aws-sdk/client-sesv2`
- `sendEmail()` — sends emails via SES with full support for: from, to, cc, bcc, subject, html, text, replyTo, headers, attachments
- Input validation: rejects missing from, to, subject, or body before calling SES
- Attachment support via raw MIME message builder (multipart/mixed with base64 attachments)
- `createDomainIdentity()` — creates SES domain identity, returns DKIM tokens
- `getDomainIdentity()` — checks domain verification status and DKIM info
- `deleteDomainIdentity()` — removes a domain identity from SES
- All functions use us-east-1 region as configured

### Test results
- 16 new unit tests passing (sendEmail: 10 tests, createDomainIdentity: 2, getDomainIdentity: 1, deleteDomainIdentity: 2, attachment: 1)
- 38 total tests passing (22 schema + 16 SES), no regressions
- make check: typecheck + lint passing

### Decisions
- Used `vi.hoisted()` for mock setup to handle Vitest module hoisting with top-level SES client instantiation
- Attachment emails use raw MIME builder; simple emails use SES Simple content format
- Validation errors thrown before any SES API call to fail fast

### Files changed
- src/lib/ses.ts — SES client with sendEmail, domain identity management, MIME builder
- tests/ses.test.ts — 16 unit tests covering all functions and edge cases

## Iteration 3 — infra-003: Cloudflare DNS Client (2026-03-29)

### What was built
- Cloudflare DNS client (`src/lib/cloudflare.ts`) using Cloudflare REST API v4
- `createDNSRecord()` — creates TXT, CNAME, or MX records with optional priority
- `listDNSRecords()` — lists DNS records with optional name/type filters
- `deleteDNSRecord()` — deletes a DNS record by ID
- `autoConfigureDomain()` — creates DKIM CNAME records for all SES DKIM tokens
- Input validation: rejects missing API token, zone ID, record ID, domain, and empty DKIM tokens
- Cloudflare API error handling with descriptive error messages

### Test results
- 15 new unit tests passing (createDNSRecord: 5, listDNSRecords: 3, deleteDNSRecord: 3, autoConfigureDomain: 3, validation: 1)
- 53 total tests passing (22 schema + 16 SES + 15 Cloudflare), no regressions
- make check: typecheck + lint passing

### Decisions
- Used global `fetch` (no extra HTTP library) for Cloudflare REST API calls
- DKIM records created as CNAME type pointing to `{token}.dkim.amazonses.com` (AWS SES standard)
- `autoConfigureDomain` creates records sequentially to avoid rate limiting
- Environment variables read at call time (not module load) to support testing

### Files changed
- src/lib/cloudflare.ts — Cloudflare DNS client with CRUD operations and auto-configure
- tests/cloudflare.test.ts — 15 unit tests covering all functions and edge cases

## Iteration 4 — infra-004: S3 Storage Client (2026-03-29)

### What was built
- S3 storage client (`src/lib/s3.ts`) using `@aws-sdk/client-s3` and `@aws-sdk/s3-request-presigner`
- `uploadFile()` — uploads files to S3 bucket, returns public URL and key
- `getPresignedUrl()` — generates presigned download URLs with configurable expiry (default 3600s)
- `deleteFile()` — deletes files from S3 by key
- Input validation: rejects missing key, body, or contentType before calling S3
- Uses bucket `resend-clone-storage-699486076867` in us-east-1

### Test results
- 15 new unit tests passing (uploadFile: 7 tests, getPresignedUrl: 4 tests, deleteFile: 4 tests)
- 68 total tests passing (22 schema + 16 SES + 15 Cloudflare + 15 S3), no regressions
- make check: typecheck + lint passing

### Decisions
- Used `vi.hoisted()` for mock setup consistent with SES test pattern
- Public URL format: `https://{bucket}.s3.{region}.amazonaws.com/{key}`
- Presigned URLs use `GetObjectCommand` with configurable expiry for secure downloads
- Supports all MIME types (image/*, text/html, application/pdf, etc.)

### Files changed
- src/lib/s3.ts — S3 client with upload, presigned URL, and delete operations
- tests/s3.test.ts — 15 unit tests covering all functions and edge cases

## Iteration 5 — layout-001: Core App Shell (2026-03-29)

### What was built
- Sidebar component (`src/components/sidebar.tsx`) with 10 nav items: Emails, Broadcasts, Templates, Audience, Metrics, Domains, Logs, API Keys, Webhooks, Settings
- Each nav item has an SVG icon matching Resend's visual design
- Team switcher at top ("foreverbrowsing" with purple avatar + gear icon)
- User section at bottom (avatar + email)
- Active nav highlighting with `data-active` attribute and distinct styling
- Dashboard layout with sidebar (fixed 250px) + content area + top header bar (Feedback, Help, Docs)
- Dark theme: #000 background, #F0F0F0 active text, #A1A4A5 inactive text, rgba(24,25,28,0.88) active bg
- Root `/` redirects to `/emails`
- Next.js App Router route group `(dashboard)` for all sidebar pages
- 10 stub pages created for each nav route

### Test results
- 5 new unit tests passing (sidebar nav config: count, labels, hrefs, uniqueness)
- 5 E2E smoke tests written (sidebar render, navigation, active highlighting, redirect, persistence)
- 73 total unit tests passing (22 schema + 16 SES + 15 Cloudflare + 15 S3 + 5 sidebar), no regressions
- make check: typecheck + lint passing

### Decisions
- Used Next.js route group `(dashboard)` for sidebar layout to keep root layout clean
- SVGs use `aria-hidden="true"` for Biome a11y lint compliance (labels provide text)
- Active state uses both visual styling and `data-active` attribute for E2E testability

### Files changed
- src/components/sidebar.tsx — Sidebar component with nav, team switcher, user section
- src/app/layout.tsx — Root layout (dark theme, updated title)
- src/app/page.tsx — Root redirect to /emails
- src/app/(dashboard)/layout.tsx — Dashboard layout with sidebar + header
- src/app/(dashboard)/page.tsx — Dashboard redirect to /emails
- src/app/(dashboard)/emails/page.tsx — Emails stub page
- src/app/(dashboard)/broadcasts/page.tsx — Broadcasts stub page
- src/app/(dashboard)/templates/page.tsx — Templates stub page
- src/app/(dashboard)/audience/page.tsx — Audience stub page
- src/app/(dashboard)/metrics/page.tsx — Metrics stub page
- src/app/(dashboard)/domains/page.tsx — Domains stub page
- src/app/(dashboard)/logs/page.tsx — Logs stub page
- src/app/(dashboard)/api-keys/page.tsx — API Keys stub page
- src/app/(dashboard)/webhooks/page.tsx — Webhooks stub page
- src/app/(dashboard)/settings/page.tsx — Settings stub page
- tests/sidebar.test.ts — 5 unit tests for sidebar nav config
- tests/e2e/smoke.spec.ts — 5 E2E smoke tests for navigation

## Iteration 6 — design-001: Shared Data Table Component (2026-03-29)

### What was built
- Reusable `DataTable<T>` generic component (`src/components/data-table.tsx`)
- Column definitions with custom `render` functions for flexible cell content
- Optional checkbox column with select-all/deselect-all in header
- Individual row checkbox toggle
- Row click handler (`onRowClick`) with keyboard accessibility (Enter/Space)
- Three-dot "More actions" button per row with dropdown menu
- Action items support `destructive` styling (red text) and `separator` (border-top divider)
- Click-outside-to-close for action dropdown menus
- Empty state message when no rows
- Dark theme styling matching Resend: #A1A4A5 headers (12px), #F0F0F0 cell text (14px), rgba(176,199,217,0.145) borders
- Hover state on rows: bg-[rgba(24,25,28,0.5)]

### Test results
- 11 new unit tests passing (render headers/rows, custom render, empty state, checkbox select all, individual checkbox, onRowClick, actions menu button, action menu items, no checkbox when disabled, column count, row count)
- 84 total tests passing (22 schema + 16 SES + 15 Cloudflare + 15 S3 + 5 sidebar + 11 data-table), no regressions
- make check: typecheck + lint passing

### Decisions
- Used generic `DataTable<T>` for type-safe reuse across all list pages (emails, domains, api-keys, etc.)
- Exported `Column<T>` and `RowAction<T>` interfaces for consuming components
- Added `onKeyDown` handler on clickable cells for Biome a11y compliance
- Used `.tsx` for test file to enable JSX with proper generic type inference

### Files changed
- src/components/data-table.tsx — Generic DataTable component with all features
- tests/data-table.test.tsx — 11 unit tests for DataTable component

## Iteration 7 — design-002: Shared Filter Bar Components (2026-03-29)

### What was built
- SearchInput component (`src/components/search-input.tsx`) — text input with magnifying glass icon, placeholder "Search...", dark theme styling with 12px border-radius
- DateRangePicker component (`src/components/date-range-picker.tsx`) — button showing selected range, popover with 6 presets (Today, Yesterday, Last 3/7/15/30 days) + calendar month view with month navigation, selected range highlighting, checkmark on active preset
- DropdownFilter component (`src/components/dropdown-filter.tsx`) — button with dropdown popover, colored dot indicators per option, checkmark on selected option, click-outside-to-close
- ComboboxFilter component (`src/components/combobox-filter.tsx`) — searchable dropdown with text filter, filters options as you type, click-outside-to-close
- ExportButton component (`src/components/export-button.tsx`) — icon button with download arrow SVG
- All components use consistent dark theme: rgba(24,25,28,0.88) bg, rgba(176,199,217,0.145) border, 12px border-radius

### Test results
- 20 new unit tests passing (SearchInput: 3, DateRangePicker: 6, DropdownFilter: 5, ComboboxFilter: 4, ExportButton: 2)
- 104 total tests passing (22 schema + 16 SES + 15 Cloudflare + 15 S3 + 5 sidebar + 11 data-table + 20 filter-bar), no regressions
- make check: typecheck + lint passing

### Decisions
- Each filter component is standalone with controlled value/onChange props for flexible composition
- DateRangePicker uses preset strings as values (not Date objects) for simplicity — consuming pages can convert to date ranges
- Used stable keys (not array indices) for calendar grid to satisfy Biome lint rules
- Click-outside-to-close pattern shared across dropdown, combobox, and date range picker
- ComboboxFilter filters options case-insensitively on the label field

### Files changed
- src/components/search-input.tsx — Search input with icon
- src/components/date-range-picker.tsx — Date range picker with presets + calendar
- src/components/dropdown-filter.tsx — Dropdown filter with colored dots
- src/components/combobox-filter.tsx — Searchable combobox filter
- src/components/export-button.tsx — Export icon button
- tests/filter-bar.test.tsx — 20 unit tests for all filter components

## Iteration 8 — design-003: Shared UI Primitives (2026-03-29)

### What was built
- Pagination component (`src/components/pagination.tsx`) — page info display, items per page selector (40/80/120) with dropdown, prev/next navigation buttons with disabled states
- Modal component (`src/components/modal.tsx`) — centered dialog with title, close button (X), Cancel button, optional action button, Escape key to close, dark overlay
- Toast component + ToastProvider (`src/components/toast.tsx`) — success/error toast notifications with icons, auto-dismiss after 3s, context-based hook (useToast) for imperative usage
- StatusBadge component (`src/components/status-badge.tsx`) — text-based status indicators with 5 variants: success (green), error (red), warning (yellow), info (blue), default (gray)
- CopyToClipboard component (`src/components/copy-to-clipboard.tsx`) — displays value with copy button, shows checkmark confirmation state for 1.5s after copying
- Tabs component (`src/components/tabs.tsx`) — tab bar with data-state active/inactive attributes, bottom border indicator on active tab
- Toggle component (`src/components/toggle.tsx`) — binary switch control with label, data-state checked/unchecked, accessible role="switch"
- EmptyState component (`src/components/empty-state.tsx`) — centered empty state with title, description, optional icon and action button
- All components use consistent dark theme styling matching Resend's design system

### Test results
- 30 new unit tests passing (Pagination: 5, Modal: 5, Toast: 3, StatusBadge: 5, CopyToClipboard: 3, Tabs: 4, Toggle: 3, EmptyState: 2)
- 134 total tests passing (22 schema + 16 SES + 15 Cloudflare + 15 S3 + 5 sidebar + 11 data-table + 20 filter-bar + 30 ui-primitives), no regressions
- make check: typecheck + lint passing

### Decisions
- Used custom modal instead of Radix Dialog to match exact Resend styling and keep test assertions simple
- Toggle uses `role="switch"` with `data-state` attribute instead of Radix Switch for consistency
- Tabs use `data-state="active"/"inactive"` for E2E testability matching Resend's pattern
- Toast uses context provider pattern (ToastProvider + useToast hook) for app-wide imperative usage
- Added `afterEach(cleanup)` to tests since vitest doesn't auto-cleanup @testing-library renders

### Files changed
- src/components/pagination.tsx — Pagination with page size selector and navigation
- src/components/modal.tsx — Modal dialog with overlay, close, cancel, action
- src/components/toast.tsx — Toast notifications with provider and hook
- src/components/status-badge.tsx — Status badge with 5 color variants
- src/components/copy-to-clipboard.tsx — Copy to clipboard with confirmation state
- src/components/tabs.tsx — Tab bar with active/inactive states
- src/components/toggle.tsx — Toggle switch with accessible role
- src/components/empty-state.tsx — Empty state with title, description, action
- tests/ui-primitives.test.tsx — 30 unit tests for all 8 UI primitives

## Iteration 9 — api-001: Email Sending REST API (2026-03-29)

### What was built
- API key authentication middleware (`src/lib/api-auth.ts`) — validates Bearer token by SHA-256 hashing and looking up in DB
- POST /api/emails — sends single email via SES, stores in DB with events, returns `{ id }`. Supports from, to (string or array), subject, html, text, cc, bcc, reply_to, headers, attachments, tags, scheduled_at
- POST /api/emails/batch — sends up to 100 emails in one request, validates each email, returns `{ data: [{ id }] }`. Rejects >100 with 400 error
- GET /api/emails — lists emails with cursor pagination (limit, after, before params), returns `{ object: "list", has_more, data }` matching Resend API format
- GET /api/emails/:id — returns single email with events, `{ object: "email", ... }` format with snake_case field names
- Input validation: 422 for missing required fields (from, to, subject, html/text), 401 for missing/invalid API key
- String `to` field auto-normalized to array for API compatibility

### Test results
- 11 new unit tests passing (auth: 2, POST send: 4, POST batch: 2, GET list: 1, GET detail: 2)
- 145 total tests passing (134 previous + 11 new), no regressions
- make check: typecheck + lint passing

### Decisions
- Mocked `api-auth` module in tests to bypass real DB auth, testing auth separately via route-level 401 checks
- Used `vi.hoisted()` pattern for mock setup consistent with existing test patterns
- API response format matches Resend API exactly: snake_case fields, `object` type field, cursor pagination with `has_more`
- normalizeToArray helper handles both string and array inputs for to/cc/bcc/reply_to fields

### Files changed
- src/lib/api-auth.ts — API key validation middleware (SHA-256 hash lookup)
- src/app/api/emails/route.ts — POST (send) and GET (list) email endpoints
- src/app/api/emails/batch/route.ts — POST batch send endpoint
- src/app/api/emails/[id]/route.ts — GET single email with events
- tests/api-emails.test.ts — 11 unit tests for all email API endpoints

## Iteration 10 — feature-030: API Keys List Page (2026-03-29)

### What was built
- API Keys list page (`src/app/(dashboard)/api-keys/page.tsx`) — server component that fetches keys and domains from DB
- `ApiKeysList` client component (`src/components/api-keys-list.tsx`) — full CRUD list page with:
  - Page title "API Keys" + "Create API Key" button (matching Resend screenshot)
  - Filter bar: SearchInput for name/token filtering, DropdownFilter for permissions (All Permissions, Full access, Sending access), ExportButton (CSV download)
  - DataTable with 5 columns: Name (link to detail), Token (truncated re_xxx... monospace), Permission, Last Used, Created
  - Checkbox column for bulk selection, three-dot row action menu with Delete
  - Pagination (40/80/120 items per page)
  - Empty state when no keys exist
  - Create API Key modal: Name input, Permission select (Full access/Sending access), Domain select (All Domains + list)
- GET /api/api-keys route — lists all API keys ordered by creation date
- POST /api/api-keys route — creates new API key with re_ prefix, SHA-256 hashed storage, returns raw key once

### Test results
- 11 new unit tests passing (page title, columns, row data, permissions, search filter, permission filter, empty state, create modal, modal fields, checkboxes, action menu)
- 156 total tests passing (145 previous + 11 new), no regressions
- make check: typecheck + lint passing

### Decisions
- Used existing shared components (DataTable, SearchInput, DropdownFilter, ExportButton, Pagination, EmptyState, Modal)
- API key generation: `re_` + UUID (no dashes), stored as SHA-256 hash, prefix shown as `re_xxxx...`
- Dashboard API routes don't require API key auth (internal use), unlike public API endpoints
- Relative time formatting for Last Used and Created columns matching Resend's "about X hours ago" style

### Files changed
- src/components/api-keys-list.tsx — ApiKeysList client component with all UI features
- src/app/(dashboard)/api-keys/page.tsx — Server component fetching keys + domains
- src/app/api/api-keys/route.ts — GET (list) and POST (create) API key endpoints
- tests/api-keys-page.test.tsx — 11 unit tests for API Keys list page

## Iteration 11 — feature-031: Create API Key Modal Behavior (2026-03-29)

### What was built
- Enhanced Create API Key modal in `ApiKeysList` component with:
  - Domain field disabled when "Full access" permission is selected, enabled for "Sending access"
  - Domain field resets to "All Domains" when switching back to Full access
  - Add button disabled until name is entered (actionDisabled prop on Modal)
  - Token reveal view after successful creation — shows raw API key once with warning "This key will never be shown again"
- Added `actionDisabled` prop to shared Modal component for disabling action button

### Test results
- 8 new unit tests passing (domain disabled for full_access, domain enabled for sending_access, domain resets on switch, domain shows verified domains, Add button disabled when empty, Add button enabled with name, token reveal after creation, token warning message)
- 164 total tests passing (156 previous + 8 new), no regressions
- make check: typecheck + lint passing

### Decisions
- Token reveal replaces the form view in the same modal after successful POST
- Modal close after token reveal triggers page reload to refresh the list
- Added `actionDisabled` prop to shared Modal component (generic, reusable for other modals)
- Permission change handler resets domain selection to prevent stale domain_id with full_access

### Files changed
- src/components/api-keys-list.tsx — Enhanced modal with domain disable logic, Add button disable, token reveal
- src/components/modal.tsx — Added actionDisabled prop for disabling action button
- tests/api-keys-modal.test.tsx — 8 unit tests for modal behavior

## Iteration 12 — feature-032: API Key Detail Page (2026-03-29)

### What was built
- API Key detail page (`src/app/(dashboard)/api-keys/[id]/page.tsx`) — server component that fetches key metadata, domain name, total uses (log count), last used timestamp
- `ApiKeyDetail` client component (`src/components/api-key-detail.tsx`) with:
  - Breadcrumb navigation (API Keys / API Key)
  - Lock icon + key name as h1 heading
  - 7 metadata fields in 4-column grid: Permission, Domain, Total Uses (linked to /logs?api_key=:id), Token (truncated), Last Used, Created, Creator (with avatar)
  - More actions dropdown: Edit API key, Go to docs, Delete API key
  - Edit modal with pre-filled Name, Permission, Domain fields + Save button
  - Delete confirmation modal with destructive action
- GET /api/api-keys/:id — returns single API key details
- PATCH /api/api-keys/:id — updates name, permission, domain_id
- DELETE /api/api-keys/:id — deletes API key and redirects to list

### Test results
- 8 new unit tests passing (metadata fields, total uses link, more actions dropdown, edit modal pre-filled, save edit, delete confirmation, lock icon, token truncated)
- 1 E2E test written (edit API key name flow)
- 172 total tests passing (164 previous + 8 new), no regressions
- make check: typecheck + lint passing

### Decisions
- Total uses count fetched via logs table COUNT query filtered by api_key_id
- Last used derived from most recent log entry for the API key
- Creator email hardcoded to jaeyunha@foreverbrowsing.com (no user auth system)
- Edit modal reuses same form pattern as Create modal with permission/domain logic
- Token always shown truncated (keyPrefix), never full key after creation

### Files changed
- src/components/api-key-detail.tsx — ApiKeyDetail client component with metadata, edit modal, delete modal
- src/app/(dashboard)/api-keys/[id]/page.tsx — Server component fetching key + domain + logs data
- src/app/api/api-keys/[id]/route.ts — GET, PATCH, DELETE endpoints for single API key
- tests/api-key-detail.test.tsx — 8 unit tests for API Key detail page
- tests/e2e/api-key-detail.spec.ts — E2E test for editing API key name

## Iteration 13 — feature-001: Emails Page Layout (2026-03-29)

### What was built
- EmailsHeader client component (`src/components/emails-header.tsx`) with:
  - "Emails" h1 page title
  - Sending/Receiving tab bar using Next.js Link for URL-based navigation
  - Active tab underline indicator with data-state attribute
  - Sending tab links to /emails, Receiving tab links to /emails/receiving
  - Three-dot More actions button with dropdown menu containing "Go to docs" link
  - Click-outside-to-close for dropdown menu
- Updated /emails page to use EmailsHeader with activeTab="sending"
- Created /emails/receiving page with EmailsHeader activeTab="receiving"

### Test results
- 8 new unit tests passing (page title, tabs render, Sending active, Receiving active, Sending href, Receiving href, More actions button, Go to docs dropdown)
- 180 total tests passing (172 previous + 8 new), no regressions
- make check: typecheck + lint passing

### Decisions
- Used Link components (not Tabs state) for tab navigation since tabs change URL routes
- EmailsHeader is a reusable client component that takes activeTab prop from server pages
- More actions dropdown uses same click-outside pattern as other components

### Files changed
- src/components/emails-header.tsx — EmailsHeader component with tabs, title, more actions
- src/app/(dashboard)/emails/page.tsx — Updated to use EmailsHeader
- src/app/(dashboard)/emails/receiving/page.tsx — New receiving page with EmailsHeader
- tests/emails-page.test.tsx — 8 unit tests for emails page layout

## Iteration 14 — feature-002: Emails Sending Tab Filter Bar (2026-03-29)

### What was built
- EmailsSendingFilterBar client component (`src/components/emails-sending-filter-bar.tsx`) with:
  - SearchInput for filtering by recipient or subject
  - DateRangePicker defaulting to "Last 15 days" with 6 presets (Today, Yesterday, Last 3/7/15/30 days)
  - Status dropdown filter with all 12 email statuses (Bounced, Canceled, Clicked, Complained, Delivered, Delivery Delayed, Failed, Opened, Scheduled, Sent, Queued, Suppressed) with colored dot indicators
  - API Key combobox filter showing "All API Keys" + dynamically loaded keys from DB
  - Export icon button (right-aligned)
- EmailsSendingPage client wrapper (`src/components/emails-sending-page.tsx`) — combines EmailsHeader + EmailsSendingFilterBar with filter state management
- Updated /emails server page to fetch API keys from DB and pass to client component
- All filter changes emit via onFiltersChange callback with full filter state object

### Test results
- 12 new unit tests passing (search input, date range default + presets, status default + all 12 statuses + colored dots, API key combobox + options, export button, onFiltersChange for search/status/dateRange)
- 3 E2E tests written (filter bar renders, status dropdown, date range preset)
- 192 total unit tests passing (180 previous + 12 new), no regressions
- make check: typecheck + lint passing

### Decisions
- Used thin client wrapper (EmailsSendingPage) to manage filter state, keeping server component for data fetching
- Status options use colored dots matching Resend's visual design (green=delivered/sent, red=bounced/failed, etc.)
- Filter bar composition reuses all shared components (SearchInput, DateRangePicker, DropdownFilter, ComboboxFilter, ExportButton)
- onFiltersChange emits full filter state object on every change for easy consumption

### Files changed
- src/components/emails-sending-filter-bar.tsx — Filter bar component with all 5 filter controls
- src/components/emails-sending-page.tsx — Client wrapper combining header + filter bar with state
- src/app/(dashboard)/emails/page.tsx — Server component fetching API keys, rendering client page
- tests/emails-filter-bar.test.tsx — 12 unit tests for filter bar
- tests/e2e/emails-filter-bar.spec.ts — 3 E2E tests for filter interactions

## Iteration 15 — feature-003: Emails Sending Tab Data Table (2026-03-29)

### What was built
- EmailsSendingDataTable client component (`src/components/emails-sending-data-table.tsx`) with:
  - 4 columns: To (avatar + email link), Status (colored badge), Subject (plain text), Sent (relative time with tooltip)
  - Avatar circle with initial letter and deterministic color based on email hash
  - Email links navigate to `/emails/:id` detail page
  - Status badges with color-coded variants (success=delivered/sent, error=bounced/failed, info=opened/clicked, warning=delivery_delayed/complained, default=queued/scheduled/canceled/suppressed)
  - Relative time formatting ("about X hours/minutes/days ago") with full datetime tooltip
  - Three-dot "More actions" button per row
  - Empty state "No emails found" when no emails
- Updated EmailsSendingPage to integrate data table with client-side filtering (search by to/subject, filter by status)
- Updated /emails server page to fetch emails from DB (ordered by created_at desc, limit 100)
- `formatRelativeTime()` and `getStatusVariant()` exported for reuse and testing

### Test results
- 8 new unit tests passing (rows with columns, status badges, email links, avatars, action buttons, empty state, relative time formatting, status variant mapping)
- 1 E2E test written (click email navigates to detail page)
- 200 total unit tests passing (192 previous + 8 new), no regressions
- make check: typecheck + lint passing

### Decisions
- Built standalone data table component rather than using generic DataTable — email table has unique cell rendering (avatar, link, badge) that doesn't fit generic column approach cleanly
- Client-side filtering for search and status applied via useMemo in EmailsSendingPage
- Relative time uses simple math (no library) for "about X ago" format matching Resend's style
- Avatar color derived from email string hash for consistent colors per recipient

### Files changed
- src/components/emails-sending-data-table.tsx — Data table component with all email-specific rendering
- src/components/emails-sending-page.tsx — Updated to include data table + client-side filtering
- src/app/(dashboard)/emails/page.tsx — Updated to fetch emails from DB
- tests/emails-data-table.test.tsx — 8 unit tests for data table
- tests/e2e/emails-data-table.spec.ts — E2E test for email navigation

## Iteration 16 — feature-004: Email Detail Page (2026-03-29)

### What was built
- EmailDetail client component (`src/components/email-detail.tsx`) with:
  - Page header: "Email" label + recipient email as h1, envelope icon
  - 4 metadata fields in grid: FROM (email), SUBJECT (text), TO (email), ID (UUID with copy button)
  - Email Events section: horizontal timeline with status badges (Sent, Delivered, etc.) + formatted timestamps
  - Content tabs: Preview (sandboxed iframe), Plain Text, HTML
  - Status badges with color-coded variants matching email status
  - More actions button in header
- Server page (`src/app/(dashboard)/emails/[id]/page.tsx`) fetching email + events from DB
- Events ordered chronologically (ASC) from emailEvents table

### Test results
- 8 new unit tests passing (metadata fields, event timeline order, copy button, page header, content tabs, preview content, timestamp format, envelope icon)
- 1 E2E test written (view email detail from list, verify metadata and events)
- 208 total unit tests passing (200 previous + 8 new), no regressions
- make check: typecheck + lint passing

### Decisions
- Used sandboxed iframe for HTML preview for security (avoids XSS from raw HTML injection)
- Event timestamps formatted as "Mar 28, 4:14 PM" matching Resend's style
- Reused CopyToClipboard and StatusBadge shared components
- Server component fetches email + events with separate queries joined by emailId

### Files changed
- src/components/email-detail.tsx — EmailDetail client component with metadata, events, content tabs
- src/app/(dashboard)/emails/[id]/page.tsx — Server component fetching email + events from DB
- tests/email-detail.test.tsx — 8 unit tests for email detail page
- tests/e2e/email-detail.spec.ts — E2E test for email detail navigation

## Iteration 17 — feature-005: Email Detail Content Tabs with Insights (2026-03-29)

### What was built
- Added Insights tab (4th tab) to email detail page content area
- 11 deliverability insight checks: DMARC record, click tracking, open tracking, subdomain usage, link URL matching, plain text version, body size, no-reply address, image hosting, SVG images, YouTube URLs
- Insights split into two sections: "NEEDS ATTENTION" (warning icon, amber) and "DOING GREAT" (checkmark icon, green)
- Each insight is an expandable accordion item showing detailed explanation on click
- Insights tab badge count showing number of needs_attention items
- Copy button next to content tabs for copying active tab's content
- InsightAccordion component with expand/collapse state and appropriate icons

### Test results
- 8 new unit tests passing (4 tabs render, Preview default active, Insights tab shows sections, insights categorization, accordion expand, badge count, copy button, all 4 tabs switching)
- 1 E2E test written (tab switching with Insights, accordion expand, section verification)
- 216 total unit tests passing (208 previous + 8 new), no regressions
- make check: typecheck + lint passing

### Decisions
- Used static DEFAULT_INSIGHTS array (not from DB) since insights are computed checks, not stored data
- DMARC is the only "needs_attention" item by default; other 10 checks are "doing_great"
- Insights content area uses dark background (not white like Preview/PlainText/HTML) matching Resend's design
- Added .omx to biome.json ignore list (external state files causing lint noise)

### Files changed
- src/components/email-detail.tsx — Added Insights tab, InsightAccordion component, copy button, DEFAULT_INSIGHTS data
- tests/email-detail-insights.test.tsx — 8 unit tests for Insights tab
- tests/email-detail.test.tsx — Updated tab test to include Insights
- tests/e2e/email-detail-insights.spec.ts — E2E test for tab switching and Insights
- tests/e2e/email-detail.spec.ts — Updated to verify Insights tab visibility
- biome.json — Added .omx to ignore list
